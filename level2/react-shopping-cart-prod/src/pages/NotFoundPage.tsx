import BottomSheet from '../components/common/bottom-sheet';

const NotFoundPage = () => {
  return (
    <div>
      <BottomSheet>
        <BottomSheet.Header />
        <BottomSheet.Content />
      </BottomSheet>
    </div>
  );
};

export default NotFoundPage;
